apiVersion: v1
kind: Service
metadata:
  name: {{ include "fabrik8.fullname" . }}
  labels:
    {{- include "fabrik8.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "fabrik8.selectorLabels" . | nindent 4 }}
